// Container for all the projects

import React from 'react';

function Projects() {
  return (
    <div>
      <h1>Projects Template that will take each child component</h1>
      <p> Create Child component with each project</p>
    </div>
  );
}

export default Projects;
